import styled from 'styled-components'

export const SCProjectsContainer = styled.div`
  padding: 10% 20% 0 20%;
  background: ${({ theme }) => theme.backgroundColor};
  min-height: 100vh;
`

export const SCTitle = styled.h1`
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 38px;
`

export const SCBackButtonContainer = styled.div`
  padding: 72px;
`
